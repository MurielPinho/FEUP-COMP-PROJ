import io;

class Test {
    public int func(int num) {
        int num_aux;
        int a;
        a = 0;

        while(a < 3){
            a = a + 1;
        }

        if (num < 1)
            if(1 + 1 < 3)
                num_aux = 1;
            else
                num_aux = a;
        else
            num_aux = (1 + 2 * 2) * 5; /* just to test */
        return num_aux;
    }


    public static void main(String[] args) {
        io.println(new Test().func(10));
    }
}
