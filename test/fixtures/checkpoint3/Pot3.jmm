import io;

class Pot3 {
    public int func(int num) {
        int num_aux;
        int a;
        a = 0;

        if(!(true && !(3<4)))
            num_aux=a;
        else
            num_aux=0;

        if (num < 1)
            if(1 < 3)
                num_aux = 1;
            else
                num_aux = a;
        else
            num_aux = (1 + 2 * 2) * 5;

        if(2<1)
            num_aux = 3;
        else
            num_aux = 4;

        return num_aux;
    }

}
